To insert a interactive R cell in the `content.html` file of a page, you can use the following syntax:

```html
 ```r-cell
 # This is an R cell
 print("Hello, Atomic Learning!")
 ```
```

This will render as the following interactive R cell:</p>

```r-cell
# This is an R cell
print("Hello, Atomic Learning!")
```
